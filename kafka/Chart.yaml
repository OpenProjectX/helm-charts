apiVersion: v2
name: kafka
description: Configurable Kafka KRaft stack with optional Kerberos, Schema Registry, Kafbat UI, topic init, and MirrorMaker 2
type: application
version: 0.1.0
appVersion: "3.9.0"
