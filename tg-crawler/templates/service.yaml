apiVersion: v1
kind: Service
metadata:
  name: tg-crawler
spec:
  selector:
    app: tg-crawler
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
  type: {{ .Values.service.type }}
